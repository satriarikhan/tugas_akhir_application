// lib/saran_page.dart
// (FILE BARU - PLACEHOLDER)

import 'package:flutter/material.dart';

class SaranPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Halaman Saran & Kesan (WIP)',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}